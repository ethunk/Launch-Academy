<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title><%= @actor_movies[0]["name"] %></title>
  </head>
  <body>
    <h1><%= @actor_movies[0]["name"] %> Movies:</h1>
    <ul>
      <% @actor_movies.each do |movie| %>
        <li><b>Movie: </b><a href="/movies/<%= movie["id"] %>"><%= movie["title"] %></a> | <b>Character: </b><%= movie["character"] %></li>
      <% end %>
    </ul>
    <p><a href="/actors">Back to List of Actors<a/></p>
  </body>
</html>
